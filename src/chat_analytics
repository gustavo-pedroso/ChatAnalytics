from src.chat import Chat

chat = Chat('data/WhatsApp Chat with .txt')

chat.get_messages()
chat.message_counts()
chat.word_counts()
chat.average_response_time()
chat.chat_restarts(1440, double_reply=True)
